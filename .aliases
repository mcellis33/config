alias lp="open /Applications/LightPaper.app"

# re-build
alias reb="make wipe clean && make"
# re-topo12
alias ret="make wipe && make && make topo12"

alias lgs="$HOME/lgs.zsh"
alias plgs="$HOME/plgs.zsh"
